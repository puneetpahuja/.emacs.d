;; -*- mode: emacs-lisp -*-
;; History entries used for helm adaptive display.
(setq helm-adaptive-history (quote (("Bookmark helm-find-files sessions" ("geekskool" ("gee" . 1)) ("brave" ("bra" . 1)) ("top" ("top" . 2)) ("emacs" ("ema" . 1))) ("Bookmark Files&Directories" ("gen" ("gen" . 1) ("ge" . 1)) ("convert" ("" . 3))) ("*helm grep extensions*" ("*" ("" . 1))))))
